# refresh_owncloud_ftp
Short python script to refresh owncloud FTP external mounts for remote modifications

Avoid to scan all files with occ files:scan --all.

Can be launched every 15 minutes with cron.
